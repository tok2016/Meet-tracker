import { Components } from '@mui/material';

const TextField: Components['MuiTextField'] = {
  defaultProps: {
    variant: 'outlined',
    fullWidth: true,
    color: 'primary',
  }
};

export default TextField;
