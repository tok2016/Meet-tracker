import MediaValue from '../types/MediaValue';

const LOGO_WIDTH: MediaValue = {
  xs: 30,
  sm: 40,
  md: 50,
  lg: 60,
  xl: 90
};

const AVATAR_WIDTH: MediaValue = {
  xs: 30,
  sm: 40,
  md: 50,
  lg: 60,
  xl: 90
};

const AVATAR_EDITOR_WIDTH: MediaValue = {
  xs: 150,
  sm: 200,
  md: 350,
  lg: 350,
  xl: 500
};

const NAV_BAR_MARGIN_BOTTOM: MediaValue = {
  xs: '12.5vh',
  sm: '12.5vh',
  md: '10vh',
  lg: '10vh',
  xl: '10vh'
};

const PAPER_SMALL_PADDING: MediaValue = {
  xs: '0.5em',
  sm: '1em',
  md: '1em',
  lg: '1.5em',
  xl: '1.5em'
};

const SIDEBAR_BUTTON_WIDTH: MediaValue = {
  xs: 30,
  sm: 40,
  md: 50,
  lg: 0,
  xl: 0
};

const ERROR_ICON_WIDTH: MediaValue = {
  xs: 55,
  sm: 70,
  md: 100,
  lg: 100,
  xl: 120
};

export {LOGO_WIDTH, AVATAR_EDITOR_WIDTH, AVATAR_WIDTH, NAV_BAR_MARGIN_BOTTOM, 
  PAPER_SMALL_PADDING, SIDEBAR_BUTTON_WIDTH, ERROR_ICON_WIDTH};
